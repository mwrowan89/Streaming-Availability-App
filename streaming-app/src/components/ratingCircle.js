import React from "react";

const ratingCircle = () => {
  return (
    <div className="progress">
      <button>Start</button>
      <button>Reset</button>
    </div>
  );
};

export default ratingCircle;
